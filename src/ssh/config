# Clave personal por defecto
Host github.com
    User git
    IdentityFile ~/.ssh/github

# Clave de trabajo solo para la carpeta del trabajo
Match exec "pwd | grep $HOME/workspace"
    Host github.com
    User git
    IdentityFile ~/.ssh/github_mapfre
